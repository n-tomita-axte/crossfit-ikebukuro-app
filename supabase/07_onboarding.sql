-- ============================================================
-- 07_onboarding.sql  —  追加分
--
-- マジックリンク・パスワードどちらでサインアップした場合も、
-- display_name が「メールアドレスの@より前」で自動生成されるだけで、
-- 本人が名前を登録する機会がありませんでした。
--
-- 初回ログイン時に「表示名を登録してもらう画面」に案内するための
-- フラグを追加します。既存ユーザーもデフォルトで false になるため、
-- 次回ログイン時に一度だけ登録画面が案内されます。
-- ============================================================
begin;

alter table public.profiles
  add column if not exists onboarded boolean not null default false;

commit;
